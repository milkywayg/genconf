add shortcuts for git
configure tmux with mouse and shorcut to ctrl+b -- done
write intsall script
powerline
config zsh
    -see two last folders of a hierarchy
    -aliases --done
fidn replacement for gitk in console mode
pyenv
pyenv virtualenv plugin
tensorflow
copy paste --done
basic git push -- done
vim:
    add my vim shortcuts (diff..)
    learn to use the folder manager plugin in vim- merdtree
    check the other plugins
    add vertical line
    add ctrl+s in vim--done
    add ctrl+s in zsh support adding the same command in some .zsh file
    add plugin that helps to edit word like files : org mode in emacs
    add vimmagit
    nerdtree see dot file hidden
    resize the panes with mouse
    plug in markers
    reload
tmux:
    copy paste with and without mouse inside tmux
    share history
    add powerline
git:
    do a shortcut "git prel" for git commit -a -m && git push
    do a way to not have to write the cerdentials everytime
urgent:
    prompt
    copy paste tmux --done
    powerline--done
    git prel --done
    github credentials --done
    vim highlight select -- done
    vim all my shortcuts --done
    chnage color of ll for folders
    share history
    vim reload -- no need change is automatic -- done
    copy paste with and without mouse inside tmux
    nerdtree see dot file hidden
    vim buffer switching and displaying -- done
    vim highlight line


